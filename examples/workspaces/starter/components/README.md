# Workspace components

Put your own components here, one folder each, with the same files as the
built-in ones: `manifest.json`, `template.html`, `style.css`, `examples/`,
and `README.md`. Run `sameway component new <name>` to scaffold one. A folder
named like a built-in component replaces it for this workspace.

# Content

Exported records live here as Markdown with front matter, one folder per
content type, so they can be reviewed, diffed, and shared through git.
Export and import commands arrive in milestone three; until then `data.db`
holds the records.

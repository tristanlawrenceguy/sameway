{
  "name": "calendar",
  "version": "1.3.0",
  "description": "A month and what is on in it, at whatever size it has been given: a marker with a count, a short agenda, a month grid, or a full page with an action beside every event; or one day by the hour. To ask someone for a date use datepicker. Given a content type, the records with a date are the events, kept current.",
  "icon": "▦",
  "use": {"when": "a person wants to see when things happen or what is coming, sized to where it sits: glance or brief in a pane, full or page in the main region.", "not": "a single date, which is a datepicker, or a list of tasks, which is a list.", "with": "events as records, so the same event is on its own page."},
  "props": {
    "type": "object",
    "additionalProperties": false,
    "required": [],
    "properties": {
      "month": {"type": "string", "description": "The month to show, as YYYY-MM, for example 2026-09. Leave out for this month."},
      "detail": {"type": "string", "enum": ["glance", "brief", "full", "page"], "default": "full", "title": "How much to show", "description": "glance is a marker with how many things are coming, for a corner or a narrow pane. brief is the next few, as a list. full is the month grid. page is the month with an action beside every event, for when the calendar has the screen to itself. Expanding a block asks for page."},
      "caption": {"type": "string", "description": "Names the calendar. Defaults to the month and year."},
      "events": {"type": "array", "description": "What is on, each on one day. Days with nothing are left empty. Filled in by the server when type is given.", "items": {"type": "object", "additionalProperties": false, "required": ["date", "label"], "properties": {"date": {"type": "string", "description": "The day, as YYYY-MM-DD."}, "label": {"type": "string", "description": "Short: what it is, not a description."}, "time": {"type": "string", "description": "Optional time, shown before the label. Filled in by the server only for a record whose date has a time of day; a day alone shows none."}, "actions": {"type": "array", "description": "Components placed inside this event, shown at page detail. Each is {component, props}: a button, a link, a badge or a mark, nothing that would trap someone inside a calendar cell. Filled in by the server for records, with the record's press.", "items": {"type": "object", "additionalProperties": false, "required": ["component", "props"], "properties": {"component": {"type": "string", "enum": ["button", "link", "badge", "mark"]}, "props": {"type": "object"}}}}, "href": {"type": "string", "description": "Where the event leads, such as a record's page. Filled in by the server for records."}, "meta": {"type": "string", "description": "A few words beside the label, such as the project it belongs to. Filled in by the server from show."}}}},
      "day": { "type": "string", "description": "One day to show instead of the month, as YYYY-MM-DD: what is on that day by the hour, all-day things first. On the canvas the day numbers of the month lead to their day." },
      "dayBase": { "type": "string", "description": "Filled in by the server on the canvas: where a day number leads, with the day appended." },
      "today": {"type": "string", "description": "A day to mark as today, as YYYY-MM-DD. Filled in by the server with today when left out; marked with text as well as colour."},
      "upcoming": {"type": "integer", "minimum": 1, "maximum": 20, "default": 5, "title": "How many to list", "description": "How many events glance counts and brief lists, from today onwards."},
      "start": {"type": "string", "enum": ["monday", "sunday"], "default": "monday", "title": "Week starts on"},
      "id": {"type": "string"},
      "type": {"type": "string", "description": "A content type whose records are the events, such as task: each record with a date on it is shown on that day, as a link to its page. Leave out to give the events yourself."},
      "date": {"type": "string", "description": "With type: the field that holds the day, such as due. Defaults to the type's first date field."},
      "where": {"type": "array", "items": {"type": "string"}, "description": "With type: conditions the records must meet, as a collection block takes them: done=false, project=<id>. Leave out for all of them."},
      "show": {"type": "array", "items": {"type": "string"}, "description": "With type: fields to show beside each event, such as project or status, by name. Leave out for the title alone."},
      "nav": {"type": "object", "additionalProperties": false, "description": "Filled in by the server on the canvas: the months either side, or the days either side and the month when one day is shown, as links to this block's own page showing them, and today. Leave out.", "properties": {"previous": {"type": "object", "additionalProperties": false, "required": ["href", "label"], "properties": {"href": {"type": "string"}, "label": {"type": "string"}}}, "next": {"type": "object", "additionalProperties": false, "required": ["href", "label"], "properties": {"href": {"type": "string"}, "label": {"type": "string"}}}, "today": {"type": "object", "additionalProperties": false, "required": ["href", "label"], "properties": {"href": {"type": "string"}, "label": {"type": "string"}}}, "month": {"type": "object", "additionalProperties": false, "required": ["href", "label"], "properties": {"href": {"type": "string"}, "label": {"type": "string"}}}}},
      "all": {"type": "string", "description": "Filled in by the server with type: the list page of the records shown. Leave out."}
    }
  },
  "a11y": {"role": "table at full and page detail, list at brief, status at glance", "keyboard": [{"key": "Tab", "does": "focuses the scroll region when the month is wider than the space, reaches the links to the months either side, the list link, and each event's actions at page detail"}, {"key": "Enter", "does": "activates a link or an event's action"}], "states": ["aria-current"], "wcag": {"target": "AAA", "notes": "Every size is a real structure rather than a styled version of the same one: a status with a count, a list, or a table whose days are announced with their weekday column header (1.3.1). Day cells name their full date, so a day read out of context still says which day it is. Today is marked with the word today as well as a colour (1.4.1) and with aria-current. At glance the count is spoken in full, so a number in a circle is never the only way to know what it means. The months either side are links in a navigation landmark named Other months, with rel prev and next, so a person moves through time without a control that needs scripting."}, "notes": "At full detail in a narrow container each event becomes a marker rather than a label: which days are busy is what a month that size can honestly answer, and the labels stay in the accessibility tree."},
  "machine": {"selector": "[data-component=\"calendar\"]", "identify": "Read data-detail for which size is being shown and data-month for the month; data-day when one day is shown. At full and page detail each day cell carries data-date; at any detail each event carries data-event and data-date.", "operate": "The calendar itself is not interactive. At page detail each event may carry its own buttons and links, which are operated like any other."},
  "examples": [
    {"name": "glance", "props": {"month": "2026-09", "detail": "glance", "today": "2026-09-11", "events": [{"date": "2026-09-11", "time": "14:00", "label": "Design review"}, {"date": "2026-09-14", "label": "Standup"}, {"date": "2026-09-16", "time": "11:00", "label": "One to one with Sam"}]}, "file": "examples/glance.html"},
    {"name": "brief", "props": {"month": "2026-09", "detail": "brief", "today": "2026-09-11", "upcoming": 3, "events": [{"date": "2026-09-11", "time": "14:00", "label": "Design review"}, {"date": "2026-09-14", "label": "Standup"}, {"date": "2026-09-16", "time": "11:00", "label": "One to one with Sam"}]}, "file": "examples/brief.html"},
    {"name": "default", "props": {"month": "2026-09", "today": "2026-09-11", "events": [{"date": "2026-09-11", "time": "14:00", "label": "Design review"}, {"date": "2026-09-14", "label": "Standup"}, {"date": "2026-09-28", "label": "Retro"}]}, "file": "examples/default.html"},
    {"name": "page", "props": {"month": "2026-09", "detail": "page", "today": "2026-09-11", "events": [{"date": "2026-09-11", "time": "14:00", "label": "Design review", "actions": [{"component": "button", "props": {"label": "Join", "context": "the design review", "variant": "quiet"}}, {"component": "link", "props": {"href": "/t/note", "label": "Notes", "context": "for the design review"}}]}, {"date": "2026-09-14", "label": "Standup", "actions": [{"component": "badge", "props": {"label": "Daily", "tone": "info"}}]}]}, "file": "examples/page.html"},
    {"name": "empty", "props": {"month": "2026-02", "start": "sunday", "caption": "February, nothing booked"}, "file": "examples/empty.html"},
    {"name": "tasks", "props": {"type": "task", "where": ["done=false"], "date": "due", "month": "2026-09", "today": "2026-09-17", "caption": "Tasks due", "events": [{"date": "2026-09-19", "label": "Order compost", "href": "/t/task/k3n2p9"}, {"date": "2026-09-22", "label": "Plant garlic", "href": "/t/task/m8q4r1"}]}, "file": "examples/tasks.html"},
    {"name": "months", "props": {"month": "2026-09", "today": "2026-09-17", "caption": "Tasks due", "events": [{"date": "2026-09-19", "label": "Order compost", "href": "/t/task/k3n2p9"}], "nav": {"previous": {"href": "/canvas/b1?month=2026-08", "label": "August 2026"}, "next": {"href": "/canvas/b1?month=2026-10", "label": "October 2026"}}, "all": "/t/task?order=due"}, "file": "examples/months.html"}
  ]
}

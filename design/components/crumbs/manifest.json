{
  "name": "crumbs",
  "version": "1.0.0",
  "description": "The way back from where a person is: the places above this one, as links, and the page itself, marked as here.",
  "icon": "›",
  "use": { "when": "a page sits inside another, such as a record inside its list.", "not": "the main navigation, or steps in a task.", "with": "the page heading, which it leads into; leave current out when the heading already says it." },
  "props": {
    "type": "object",
    "additionalProperties": false,
    "required": ["items"],
    "properties": {
      "items": {
        "type": "array",
        "minItems": 1,
        "description": "The places above this page, outermost first.",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["href", "label"],
          "properties": {
            "href": { "type": "string" },
            "label": { "type": "string" },
            "dot": { "type": "integer", "minimum": 1, "maximum": 6, "description": "The colour of the list this place is, drawn before it." }
          }
        }
      },
      "current": { "type": "string", "description": "This page's name, marked as the current page. Leave out when the heading below says it." },
      "label": { "type": "string", "default": "Breadcrumb", "description": "Names the navigation landmark. Breadcrumb is the name screen reader users know it by; You are here would say they are on the place it leads back to." }
    }
  },
  "a11y": {
    "role": "navigation holding a list of links, the last item aria-current=page",
    "keyboard": [{ "key": "Tab", "does": "reaches each link in turn" }, { "key": "Enter", "does": "goes to that place" }],
    "states": ["aria-current"],
    "wcag": { "target": "AAA", "notes": "A named navigation landmark (1.3.1) with the trail as an ordered list, so its length is announced. The chevrons between places are drawn with CSS borders, not characters, so nothing is read for them. The current page, when given, is plain text with aria-current, set apart from the links by having no underline, not by colour alone (1.4.1). Long names wrap rather than run off a phone (1.4.10). Links reach a 44px target (2.5.5)." }
  },
  "machine": {
    "selector": "[data-component=\"crumbs\"]",
    "identify": "Each link is a place above this page, outermost first; [aria-current=page] is the page itself.",
    "operate": "Follow a link to go back to that place."
  },
  "examples": [
    { "name": "default", "props": { "items": [{ "href": "/t/note", "label": "Notes", "dot": 2 }] }, "file": "examples/default.html" },
    { "name": "current", "props": { "items": [{ "href": "/t/task", "label": "Tasks" }], "current": "Order compost" }, "file": "examples/current.html" }
  ]
}

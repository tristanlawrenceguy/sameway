{
  "name": "mark",
  "version": "1.0.0",
  "description": "A checkbox that sets one yes-or-no field on one record: done, pinned, read. It says the fact (checked or not) and saves on change; without scripts a Save button beside it does the same. A form, so it works everywhere and comes back to the page it was on.",
  "icon": "✓",
  "use": {
    "when": "a person wants to change a record's state where they see it: tick a task in a list, pin a note on its page.",
    "not": "changing several fields at once, which is the inline editor; or anything that is not the person's own content.",
    "with": "a collection, a record block, a calendar event, or a record's own page."
  },
  "props": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "type",
      "record",
      "field",
      "label"
    ],
    "properties": {
      "type": {
        "type": "string",
        "minLength": 1,
        "description": "The content type of the record."
      },
      "record": {
        "type": "string",
        "minLength": 1,
        "description": "The record's id."
      },
      "field": {
        "type": "string",
        "minLength": 1,
        "description": "The field the press sets."
      },
      "label": {
        "type": "string",
        "minLength": 1,
        "description": "What the field is called, as the person reads it: Done, Pinned."
      },
      "context": {
        "type": "string",
        "description": "Which record, for screen readers: the record's title. Read after the label."
      },
      "id": {
        "type": "string"
      },
      "checked": {
        "type": "boolean",
        "default": false,
        "description": "Whether the field is true now."
      }
    }
  },
  "a11y": {
    "role": "checkbox",
    "keyboard": [
      {
        "key": "Tab",
        "does": "reaches the checkbox, and the Save button when scripts are off"
      },
      {
        "key": "Space",
        "does": "changes it, which saves when scripts run"
      }
    ],
    "states": [
      "checked"
    ],
    "wcag": {
      "target": "AAA",
      "notes": "A native checkbox inside its label, so the state is announced as a fact (checked or not) with the field's name and, when context is given, the record's title (1.3.1, 4.1.2), and a list of ten is ten different names (2.4.6). A checkbox that saves on change would surprise someone who cannot see the page settle, so the page comes back with the record glowing where it shows; without scripts a Save button does the same in one more step. A switch would say the same with less support; a button would say an action rather than the fact."
    }
  },
  "machine": {
    "selector": "[data-component=\"mark\"]",
    "identify": "Read data-record-type, data-record-id and data-field for which field of which record; the checkbox's checked state is the value now.",
    "operate": "Submit the form with the checkbox as you want it, or POST prop-<field>=true|false to /t/<type>/<id>/props yourself; the API at /api/<type>/<id> takes the same change as JSON."
  },
  "examples": [
    {
      "name": "default",
      "props": {
        "type": "task",
        "record": "k3n2p9",
        "field": "done",
        "label": "Done",
        "context": "Order compost"
      },
      "file": "examples/default.html"
    },
    {
      "name": "checked",
      "props": {
        "type": "task",
        "record": "k3n2p9",
        "field": "done",
        "label": "Done",
        "checked": true,
        "context": "Order compost"
      },
      "file": "examples/checked.html"
    }
  ]
}

{
  "name": "collection",
  "version": "1.0.0",
  "description": "The records that match a query, as a live list: the tasks due this week, the notes tagged garden, the drafts. The same few words pick them here, on the list page, in the assistant's find_records and on the command line.",
  "icon": "≡",
  "use": {
    "when": "a person wants to see what matches, kept current as records change: what is due, what is open, what is tagged.",
    "not": "one record on its own, which is a record block; or a search for words, which is the search block.",
    "with": "a heading in label, and a record block or the list page for the full thing."
  },
  "props": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "type"
    ],
    "properties": {
      "type": {
        "type": "string",
        "minLength": 1,
        "description": "The content type, such as task."
      },
      "where": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Conditions, all of which must hold. Each is field, operator, value with no spaces: status=draft, done=false, due<today, due<=+7d, title~garden (contains), tags=health (has), notes= for an empty field, due!= for a set one. Dates: 2026-10-01, today, tomorrow, yesterday, now, +7d, -1w. Leave out for every record."
      },
      "order": {
        "type": "string",
        "description": "A field to order by, or -field for the largest or newest first; created_at and updated_at work too. Newest first when left out."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100,
        "default": 20,
        "description": "How many to show at most."
      },
      "detail": {
        "type": "string",
        "enum": [
          "brief",
          "full"
        ],
        "default": "brief",
        "description": "brief is each title as a link with its state; full adds the text."
      },
      "label": {
        "type": "string",
        "description": "The heading over the list, saying what it is: Due this week."
      },
      "level": {
        "type": "integer",
        "minimum": 2,
        "maximum": 4,
        "default": 3,
        "description": "Heading level of the label so the block fits the page outline."
      },
      "items": {
        "type": "array",
        "description": "Filled in by the server: the matching records. Leave out.",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "title",
            "href"
          ],
          "properties": {
            "title": {
              "type": "string"
            },
            "href": {
              "type": "string"
            },
            "meta": {
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          }
        }
      },
      "summary": {
        "type": "string",
        "description": "Filled in by the server: the conditions in words, shown when nothing matches. Leave out."
      },
      "all": {
        "type": "string",
        "description": "Filled in by the server: the list page with the same query. Leave out."
      },
      "problem": {
        "type": "string",
        "description": "Filled in by the server when a condition is wrong, saying what the type has instead. Leave out."
      },
      "id": {
        "type": "string"
      }
    }
  },
  "a11y": {
    "role": "region",
    "keyboard": [
      {
        "key": "Tab",
        "does": "reaches each record's link, then the link to the list page with the same query"
      }
    ],
    "states": [],
    "wcag": {
      "target": "AAA",
      "notes": "The label is a real heading at the level the outline needs (1.3.1) and names the list for screen readers. Each item is a link with the record's title as its text (2.4.4). An empty result says so in words with the conditions, and a wrong condition says what the type has (3.3.1), rather than rendering nothing."
    }
  },
  "machine": {
    "selector": "[data-component=\"collection\"]",
    "identify": "Read data-record-type for the type and data-where for the conditions. Each li is one matching record; its a href is the record's page.",
    "operate": "Change what is shown by updating the block's where, order, limit and detail props. The link with class sw-collection__all opens the list page with the same query, where ?where= and ?order= carry it."
  },
  "examples": [
    {
      "name": "default",
      "props": {
        "type": "task",
        "where": [
          "done=false",
          "due<=+7d"
        ],
        "order": "due",
        "label": "Due this week",
        "items": [
          {
            "title": "Order compost",
            "href": "/t/task/k3n2p9",
            "meta": "Due 2026-09-19"
          },
          {
            "title": "Dig the pond",
            "href": "/t/task/m8q4r1",
            "meta": "Due 2026-09-21"
          }
        ],
        "all": "/t/task?where=done%3Dfalse&where=due%3C%3D%2B7d&order=due"
      },
      "file": "examples/default.html"
    },
    {
      "name": "full",
      "props": {
        "type": "note",
        "where": [
          "tags=garden"
        ],
        "detail": "full",
        "label": "Garden notes",
        "level": 2,
        "items": [
          {
            "title": "Beds",
            "href": "/t/note/a1",
            "meta": "Draft",
            "text": "Three of them, raised.\n\nCompost in October."
          }
        ],
        "all": "/t/note?where=tags%3Dgarden"
      },
      "file": "examples/full.html"
    },
    {
      "name": "empty",
      "props": {
        "type": "task",
        "where": [
          "done=false",
          "due<today"
        ],
        "label": "Overdue",
        "summary": "done=false, due<today",
        "all": "/t/task?where=done%3Dfalse&where=due%3Ctoday"
      },
      "file": "examples/empty.html"
    },
    {
      "name": "problem",
      "props": {
        "type": "task",
        "where": [
          "owner=me"
        ],
        "label": "Mine",
        "problem": "task has no field \"owner\"; it has title, done, due, notes, tags, created_at, updated_at"
      },
      "file": "examples/problem.html"
    }
  ]
}

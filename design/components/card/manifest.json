{
  "name": "card",
  "version": "1.0.0",
  "description": "A titled block of content, optionally linking somewhere. Good for one item in a collection or a summary panel.",
  "props": {
    "type": "object",
    "additionalProperties": false,
    "required": ["title"],
    "properties": {
      "title": { "type": "string", "minLength": 1 },
      "body": { "type": "string", "format": "multiline", "description": "Plain text. Blank lines start a new paragraph." },
      "meta": { "title": "Small print", "type": "string", "description": "Small secondary line such as a date or status." },
      "href": { "title": "Link", "type": "string", "description": "When set, the title becomes a link to this URL." },
      "level": { "title": "Heading level", "type": "integer", "minimum": 2, "maximum": 6, "default": 3, "description": "Heading level of the title so cards fit the page outline." },
      "id": { "type": "string" }
    }
  },
  "a11y": {
    "role": "article",
    "keyboard": [ { "key": "Tab", "does": "moves focus to the title link when href is set" } ],
    "states": [],
    "wcag": { "target": "AAA", "notes": "The whole card is not a link; only the title is, so the link text stays meaningful (2.4.9) and the body remains selectable." }
  },
  "machine": {
    "selector": "[data-component=\"card\"]",
    "identify": "An article element whose heading is the title. Read the heading text and the href on its link if present.",
    "operate": "If the title is a link, activate it to open the item."
  },
  "examples": [
    { "name": "default", "props": { "title": "Grocery list", "body": "Milk, eggs, bread.", "meta": "Updated today" }, "file": "examples/default.html" },
    { "name": "linked", "props": { "title": "Read the architecture", "href": "/t/note/abc123", "level": 2 }, "file": "examples/linked.html" }
  ]
}

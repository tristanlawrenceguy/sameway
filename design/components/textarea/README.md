# textarea

Use a textarea for more than one line of input: a message, a note body, a
description. Enter inserts a newline; the form's submit button sends. Shares
its stylesheet classes with `text-field`.

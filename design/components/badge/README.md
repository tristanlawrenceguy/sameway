# badge

Use a badge to say who did something ("Added by assistant") or what state a
thing is in ("Draft", "Failed"). The label carries the meaning; the tone
only colours it. Provenance tones (human, assistant, system) also set
`data-actor`, which is the attribute the rest of the system uses to tell
people's actions from the model's.

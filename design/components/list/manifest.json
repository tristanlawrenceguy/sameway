{
  "name": "list",
  "version": "1.0.0",
  "description": "A bulleted or numbered list of short text items. Use ordered for steps or rankings.",
  "props": {
    "type": "object",
    "additionalProperties": false,
    "required": ["items"],
    "properties": {
      "items": { "type": "array", "minItems": 1, "items": { "type": "string" } },
      "ordered": { "title": "Numbered", "type": "boolean", "default": false },
      "label": { "title": "List name", "type": "string", "description": "Accessible name for the list when there is no heading right before it." },
      "id": { "type": "string" }
    }
  },
  "a11y": {
    "role": "list",
    "keyboard": [],
    "states": [],
    "wcag": { "target": "AAA", "notes": "Real ul/ol markup so assistive tech announces item counts and positions (1.3.1)." }
  },
  "machine": {
    "selector": "[data-component=\"list\"]",
    "identify": "A ul or ol. Each li is one item. aria-label names the list when present.",
    "operate": "Not interactive."
  },
  "examples": [
    { "name": "default", "props": { "items": ["Milk", "Eggs", "Bread"], "label": "Groceries" }, "file": "examples/default.html" },
    { "name": "ordered", "props": { "items": ["Install Go", "Run sameway init", "Run sameway serve"], "ordered": true }, "file": "examples/ordered.html" }
  ]
}

# select

Use a select for one choice from a short, fixed list such as a status or a
category. It is a native control on purpose: every screen reader and browser
already knows how to drive it. For more than about ten options consider a
text field with a hint instead.
